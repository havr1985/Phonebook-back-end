const isBody = require('./isBody');
const isValidId = require('./isValidId');
const authenticate = require('./authenticate');
const upload = require('./upload');
const sizeChange = require('./sizeChange');

module.exports = {
    isBody,
    isValidId,
    authenticate,
    upload,
    sizeChange,
}